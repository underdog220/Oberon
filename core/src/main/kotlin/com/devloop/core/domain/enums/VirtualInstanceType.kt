package com.devloop.core.domain.enums

enum class VirtualInstanceType {
    /** Fokusraum fuer ein einzelnes Projekt. */
    PROJECT_FOCUS,
    /** Thematischer Fokusraum (z. B. "MES Operations"). */
    TOPIC_FOCUS,
    /** Projektubergreifender Fokusraum (Portfolio). */
    CROSS_PROJECT,
}
